{
  "name": "electron-app",
  "version": "0.0.1",
  "description": "Code Snippets on Steroids",
  "main": "dist/app.js",
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "build": "./node_modules/.bin/tsc",
    "build-image": "electron-builder --mac --windows --linux",
    "release": "electron-builder --mac --windows --linux --publish always",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/jest": "^28.1.6",
    "electron-builder": "^23.3.3",
    "jest": "^28.1.3",
    "typescript": "^4.7.4"
  },
  "jest": {
    "verbose": true,
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  },
  "build": {
    "appId": "io.codiga.assistant",
    "productName": "Codiga",
    "files": [
      "./dist/**/*"
    ],
    "mac": {
      "category": "public.app-category.developer-tools"
    },
    "dmg": {
      "icon": false
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Development"
    }
  },
  "dependencies": {
    "electron-updater": "^5.2.1"
  }
}
